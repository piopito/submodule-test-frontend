import { HomeFacade } from './home.facade';

// TODO: fix this test

describe('HomeFacade', () => {
    it('should create an instance', () => {
        expect(new HomeFacade()).toBeTruthy();
    });
});
